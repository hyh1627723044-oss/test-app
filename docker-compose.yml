services:
  api:
    build:
      context: ./backend
    ports:
      - "8000:8000"
    env_file:
      - ./backend/.env.example
    environment:
      DATABASE_URL: sqlite:////app/data/app.db
    volumes:
      - backend_data:/app/data
    restart: unless-stopped

volumes:
  backend_data:
